name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm install
        env:
          ELECTRON_GET_USE_PROXY: true
          GLOBAL_AGENT_HTTPS_PROXY: ${{ secrets.HTTPS_PROXY }}

      - name: Rebuild native modules
        run: npx electron-rebuild -f -w better-sqlite3

      - name: Build Windows Portable
        run: |
          npx electron-builder install-app-deps
          npx electron-builder --win portable --x64 --publish=never
        env:
          ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES: true

      - name: Build Windows ZIP (绿色版)
        run: |
          npx electron-builder --win zip --x64 --publish=never
        env:
          ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES: true

      - name: Upload Windows Portable
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: windows-portable
          path: release/*.exe
          if-no-files-found: warn

      - name: Upload Windows ZIP (绿色版)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: windows-zip
          path: release/*.zip
          if-no-files-found: warn

  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm install
        env:
          ELECTRON_GET_USE_PROXY: true
          GLOBAL_AGENT_HTTPS_PROXY: ${{ secrets.HTTPS_PROXY }}

      - name: Build macOS
        run: |
          npx electron-builder install-app-deps
          npx electron-builder --mac zip --x64 --publish=never
        env:
          ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES: true

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: macos-build
          path: release/*
          if-no-files-found: warn

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libnotify-dev libnss3 libxss1 libasound2t64
          npm install
        env:
          ELECTRON_GET_USE_PROXY: true
          GLOBAL_AGENT_HTTPS_PROXY: ${{ secrets.HTTPS_PROXY }}

      - name: Build Linux
        run: |
          npx electron-builder install-app-deps
          npx electron-builder --linux AppImage --x64 --publish=never
        env:
          ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES: true

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: linux-build
          path: release/*
          if-no-files-found: warn

  # 手动下载 Artifact 即可，不需要自动发布 Release
  # 如需发布 Release，请手动上传 Artifact
